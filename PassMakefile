all: FunctionInfo.so

CXX=clang
CXXFLAGS = -rdynamic $(shell llvm-config-6.0 --cxxflags) -g -O0

%.so: %.o
	$(CXX) -dylib -shared $^ -o $@
clean:
	rm -f *.o *~ *.so
opt-6.0 -load /home/seventeen/cowflow/testPass.so -passtest test.ll